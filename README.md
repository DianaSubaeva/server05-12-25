# Инструкция по использованию системы сравнения сокет-серверов

## Автоматическая настройка

При первом запуске система автоматически:

1. **Создаст тестовые файлы** (100 файлов по 500 строк в папке `test_files/`)
2. **Выполнит оценку памяти** для 1000 одновременных подключений
3. **Запустит оба сервера:**
   - Асинхронный сервер на порту **8888**
   - Многопоточный сервер на порту **8889**
4. **Выдаст рекомендации** по минимальной памяти

## Основные шаги работы

### Шаг 1: Полный запуск (рекомендуется)

1. **Запустите серверы** (в первом терминале):
   ```bash
   python run_servers.py
   ```

2. **Дождитесь сообщения:**
   ```
   СЕРВЕРЫ ЗАПУЩЕНЫ УСПЕШНО!
      Асинхронный сервер: localhost:8888
      Многопоточный сервер: localhost:8889
   ```

3. **Запустите тестирование** (во втором терминале):
   ```bash
   python client.py --requests 10 50 100 200
   ```

### Шаг 2: Просмотр результатов

После тестирования система автоматически:

1. **Выведет таблицы сравнения** в консоль
2. **Создаст графики производительности** в файле `сравнение_производительности.png`
3. **Покажет ключевые метрики:**
   - Среднее время ответа для каждого сервера
   - Процент успешных запросов
   - Пропускную способность (запросов в секунду)

### Шаг 3: Анализ рекомендаций по памяти

Система предоставит четкие рекомендации:

```
🎯 МИНИМАЛЬНАЯ ГАРАНТИРОВАННАЯ ПАМЯТЬ ДЛЯ 1000 ЗАПРОСОВ:
   ├─ Асинхронный сервер: 2.0 GB
   └─ Многопоточный сервер: 4.0 GB
```

### Шаг 4: Завершение работы

Нажмите **Ctrl+C** в терминале с серверами для корректного завершения.

## ⚙️ Дополнительные возможности

### Запуск с разными нагрузками

```bash
# Тестирование с малыми нагрузками
python client.py --requests 5 10 20

# Тестирование с большими нагрузками
python client.py --requests 100 200 500 1000

# Тестирование конкретной команды
python client.py --requests 50 --command "COUNT_FILE file_1.txt"
```

